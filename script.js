document.addEventListener('DOMContentLoaded',()=>{const reduce=window.matchMedia('(prefers-reduced-motion: reduce)').matches;document.body.classList.add('js-ready');const selectors=['.section-head','.cards article','.products-copy','.product-tile','.about .visual','.about > div:last-child','.grid div','.testimonial > *','.contact > div','footer > *'];const els=document.querySelectorAll(selectors.join(','));els.forEach((el,i)=>{el.classList.add('reveal');el.style.setProperty('--reveal-delay',`${Math.min(i%6,5)*75}ms`)});if(reduce){els.forEach(el=>el.classList.add('is-visible'));return}const observer=new IntersectionObserver(entries=>entries.forEach(entry=>{if(entry.isIntersecting){entry.target.classList.add('is-visible');observer.unobserve(entry.target)}}),{threshold:.1,rootMargin:'0px 0px -35px'});els.forEach(el=>observer.observe(el));const header=document.querySelector('header');const heroMark=document.querySelector('.hero-monogram');const aboutMark=document.querySelector('.about:before');const scroll=()=>{header?.classList.toggle('scrolled',window.scrollY>24);if(heroMark)heroMark.style.transform=`translateY(${window.scrollY*.055}px)`};scroll();window.addEventListener('scroll',scroll,{passive:true});document.querySelectorAll('.cards article,.contact-card,.grid div,.product-tile').forEach(card=>{card.addEventListener('pointermove',e=>{if(e.pointerType==='touch')return;const r=card.getBoundingClientRect(),x=(e.clientX-r.left)/r.width-.5,y=(e.clientY-r.top)/r.height-.5;card.style.setProperty('--mx',`${x*5}px`);card.style.setProperty('--my',`${y*5}px`)});card.addEventListener('pointerleave',()=>{card.style.setProperty('--mx','0px');card.style.setProperty('--my','0px')})});});